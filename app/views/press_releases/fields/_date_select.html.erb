<%= render 'press_releases/fields/handle_date' %>

<div class="question" id="q<%= field_counter+2 %>">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xs-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title text-left"><%= field.question.gsub(/CompanyName/, "#{@press_room.company_name}").gsub(/CompanyName's/, "#{@press_room.company_name}") %></h3>
                    </div> <!-- / .panel-heading -->
                    <div class="panel-body">
						<% if @date.blank? %>
						<%= f.date_select field.name %>
						<% else %>
						<%= f.date_select field.name, selected: @date %>
						<% end %>
                    </div> <!-- / .panel-body -->
                </div> <!-- / .panel -->
            </div> <!-- / .col-xs-12 -->
        </div> <!-- / .row -->
    </div> <!-- / .container-fluid -->
</div> <!-- / .question -->